<body>
<div>
      <% # formulario de cadastramento%>
      <%= form_with model: @cliente, scope: :cliente, local: true do |form| %>

        <span>
					Sign Up
				</span>

        <div>
          <%= form.label 'Name' %><br>
          <%= form.text_field :nome, placeholder:"Name" %>
        </div>

        <div>
          <%= form.label 'ID'%><br>
          <%= form.text_field :identificador, placeholder:"ID" %>
        </div>

        <div>
          <%= form.label 'Telephone'%><br>
          <%= form.text_field :telefone, placeholder:"Telephone" %>
        </div>

        <div>
          <%= form.label 'Cellphone' %><br>
          <%= form.text_field :celular, placeholder:"Cellphone" %>
        </div>

        <div>
          <%= form.label 'Email'%><br>
          <%= form.text_field :email, placeholder:"E-mail (minim 5 chars)" %>
        </div>

        <div>
          <%= form.label 'Password'%><br>
          <%= form.text_field :senha, type:"password", placeholder:"Password (minim5 chars)" %>
        </div>

        <% if(ClientesController.getResultadoPositivoCliente != "")
             valor = ""
             resultado = ClientesController.getResultadoPositivoCliente
          if resultado.split('-')[0] == 'erro'
            valor = "color: red"
            resultado = resultado.split('-')[1]
          end
        %>
          <span>
            <%= resultado %>
          </span>
          <%ClientesController.setResultadoPositivoCliente("") %>
        <% end %>

        <div>
          <button>
						<span>
							Add
						</span>
          </button>
        </div>

        <% if @cliente.errors.any? %>
          <div>
            <h2>
              <%= pluralize(@cliente.errors.count, "erros") %>
              antes de criar o cliente:
            </h2>
            <ul>
              <% @cliente.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

      <% end %>
  <button ><%= link_to 'Sign in', logins_index_path%></button>
</div>
</body>
